# LangGraph core
langgraph>=0.2.0
pydantic>=2.0.0
langchain-core>=0.3.0

# LLM (Ollama via OpenAI-compatible API)
openai>=1.0.0

# Web UI (ARM compatible, no complex build dependencies)
flask>=3.0.0
requests>=2.31.0

# Optional: Streamlit (may not work on ARM Windows)
# streamlit>=1.28.0
